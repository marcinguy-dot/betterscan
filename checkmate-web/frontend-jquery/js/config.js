/* Runtime configuration for the jQuery frontend.
 *
 * Edit this single value to point the static site at a different backend.
 * No build step or environment variables required.
 */
window.CHECKMATE_API = "http://localhost:8080";
